// For the Results Viewer Page
function compute(options, budget){

}

function options(optionObj, budget){
    const optionReq = new Array(budget + 1).fill(null).map((_) => []);
    const options = optionObj.map(({ optionId }) => parseInt(optionId));
    for (let i = 0; i < l; i++) {
        const element = array[i];
        
    }

}